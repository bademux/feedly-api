subprojects {
    apply plugin: 'idea'

    repositories {
        mavenCentral()
    }

    idea {
        module {
            downloadJavadoc = true
            downloadSources = true
        }
        project { jdkName = '1.7' }
    }
}
